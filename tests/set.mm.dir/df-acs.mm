
axiom df-acs
  let vx: setvar x
  let vf: setvar f
  let vs: setvar s
  let vc: setvar c
  assert |- ACS = ( x e. _V |-> { c e. ( Moore ` x ) | E. f ( f : ~P x --> ~P x /\ A. s e. ~P x ( s e. c <-> U. ( f " ( ~P s i^i Fin ) ) C_ s ) ) } )
end
