
axiom df-ana
  let vx: setvar x
  let vf: setvar f
  let vs: setvar s
  assert |- Ana = ( s e. { RR , CC } |-> { f e. ( CC ^pm s ) | A. x e. dom f x e. ( ( int ` ( ( TopOpen ` CCfld ) |`t s ) ) ` dom ( f i^i ( +oo ( s Tayl f ) x ) ) ) } )
end
